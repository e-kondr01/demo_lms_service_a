models:
  student:
    aggr:
      name: name
      age: age
      id: student_id
    aggr2:
      score: student_score
      id: student_id

rules:
  student:
    on_delete: set_null | cascade
    on_create: create | do_nothing
